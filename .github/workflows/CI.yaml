name: CI

on:
  push:
    paths-ignore:
      - 'README.md'
      - '.github/**'
    branches: [ main ]
  pull_request:
    paths-ignore:
      - 'README.md'
      - '.github/**'
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    uses: freddydk/myasa/.github/workflows/build.yaml@main

  cleanup:
    runs-on: windows-latest

    steps:
      - name: Cleanup
        if: always()
        uses: freddydk/myghgo/PipelineCleanup@main

